
// This file previously contained mock job data.
// It has been removed as the application now fetches job data dynamically.
// The JobListing type definition, previously implicitly used here, is managed in src/types/index.ts.

// export const mockJobs: any[] = []; // Keeping the file temporarily for structure, can be deleted.
