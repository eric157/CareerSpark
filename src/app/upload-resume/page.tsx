// This file is no longer needed as resume upload is integrated into the main page.
// It is temporarily a valid empty component to prevent build/runtime errors.
// This route and file should be deleted.
import type { NextPage } from 'next';

const UploadResumePage: NextPage = () => {
  // This page should not be accessible.
  // Its content was moved to the main dashboard page.
  return null;
};

export default UploadResumePage;